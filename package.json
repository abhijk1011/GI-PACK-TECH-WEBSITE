{
  "name": "gi-packtech-website",
  "version": "1.0.0",
  "private": true,
  "description": "GI PackTech — industrial flexible packaging website, rendered to static files",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "serve": "node scripts/serve.js",
    "start": "npm run build && npm run serve",
    "smoke": "node scripts/smoke.js",
    "images:sync": "node scripts/sync-image-files.js"
  },
  "dependencies": {
    "@sveltia/cms": "^0.189.0",
    "ejs": "^3.1.10",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
